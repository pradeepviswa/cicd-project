name: Deploy EC2 and Configure Apache

on:
  workflow_dispatch:

jobs:
  create-ec2:
    uses: ./.github/workflows/terraform-aws-create.yml
    secrets: inherit

  install-apache:
    needs: create-ec2
    uses: ./.github/workflows/ansible-aws-install-apache.yml
    secrets: inherit