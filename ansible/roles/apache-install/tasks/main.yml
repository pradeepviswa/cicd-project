---
# tasks file for apache-install

- name: Install apcache on Debian-os
  become: yes
  ansible.builtin.apt:
    name: apache2
    state: present
    update_cache: yes
  when: ansible_facts['os_family'] == "Debian"
  notify: restart_apache2